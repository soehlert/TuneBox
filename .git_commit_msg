feat: enhance stats role filtering, add admin logout, and rename jukebox references to TuneBox

- Track client connection role and display mapping inside stats:user_roles Redis hash table.
- Retrieve roles on statistics queries, enabling dynamic filtering by role ('admin' and 'display') rather than hardcoded names.
- Style and implement a logout action on the Host/Admin top-right badge pill.
- Replace Jukebox references with TuneBox on user-facing UI labels, buttons, fields, and descriptions.
- Document Jukebox Party Stats & Leaderboard feature in README.md.
